if RUBY_PLATFORM != 'opal'
  require File.join(File.dirname(__FILE__), "../../app/volt/tasks/channel_tasks")

  describe ChannelTasks do
    before do
    end
    
    it "should let channels be added and removed" do
      # a = double('channel one')
      # @channel_task = ChannelTasks.new()
      # @channel_task.add_listener('channel1', )
    end
  end
end